#!/usr/bin/env bash
set -euo pipefail
cd /srv/support/uploads
find . -mindepth 1 -maxdepth 1 -type f -printf '%P\0' |
  tar --null --verbatim-files-from --files-from=- -czf /var/backups/support/latest.tgz
